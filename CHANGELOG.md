# Журнал изменений

Записи ведутся по правилам `knowledge/00_ПРАВИЛА.md`, разделы 5, 7, 8.
Формат строки: дата — что изменилось — почему.

---

## 2026-09-26 — ST-Link без сайта ST: драйвер и OpenOCD

Записи базы, добытые на машине, где `st.com` недоступен, а зонд
ST-LINK/V2 стоял неопознанным. Кода библиотеки не касаются: `mcukit.stlink`
по-прежнему рассчитан на `STM32_Programmer_CLI`.

* **[30-10] Драйвер ST-Link берётся из установщика Keil MDK.** Без
  драйвера зонд стоит с кодом 28 (`CM_PROB_FAILED_INSTALL`), класс
  вендорский, встроенного драйвера у Windows для него нет, и зонда не
  видит никто. Подписанный STMicroelectronics пакет лежит внутри
  `MDK<версия>.EXE` в `ARM\STLink\USBDriver\` и извлекается 7-Zip.
  Ставится **`pnputil /add-driver ... /install`**; приложенный
  `dpinst_amd64.exe /q` на Windows 10 x64 молча не ставит ничего
  (`0x80030000`, в журнале `0xE0000242`). Zadig не нужен и вреден: после
  него зонд перестаёт видеть STM32CubeProgrammer.
* **[10-05] OpenOCD как замена STM32CubeProgrammer для чтения.**
  Ставится из winget, работает с ST-Link на родном драйвере ST. Транспорт
  в 0.12 — `swd`; `hla_swd` из примеров уже не поддерживается. `init`
  ядро **не останавливает**, поэтому живую плату можно осматривать, не
  тревожа прошивку. Проверено на Cortex-M0+: зонд, напряжение на цели,
  ядро, `DBGMCU_IDCODE`, объём флеша, начало флеша. Адрес IDCODE у L0 —
  `0x40015800`, а не `0xE0042000` из примеров для F1/F4. **Запись через
  OpenOCD не проверялась.**

## 2026-09-25 — второй зонд: ST-Link

* **Модуль `mcukit.stlink`** — второй, независимый от J-Link путь к
  плате, через `STM32_Programmer_CLI.exe` (STM32CubeProgrammer): `run`,
  `Result`, `probe`, `info`, `read32`, `watch`, `save`, `reset_run`,
  `flash`. Только `mk.stlink.*` — плоские имена `mk.*` по-прежнему
  J-Link, обратной несовместимости нет. Весь набор, включая `flash` и
  `reset_run`, проверен реальной записью на плате с рабочей прошивкой:
  `save()` всего флеша → тестовый образ без обращения к периферии
  (`templates/counter_l0`) → доказательство по счётчику → запись
  сохранённого дампа обратно → повторное чтение совпало побайтово.
* **`mode=HOTPLUG` — обязательное умолчание для чтения.** Обнаружено тем
  же тестом: обычный `connect` (умолчание CLI, `mode=NORMAL`) сам
  сбрасывает ядро — растущий счётчик обнулялся между подключениями.
  `info`/`read32`/`watch`/`save` подключаются `HOTPLUG` по умолчанию;
  `flash`/`reset_run` — обычным режимом нарочно. Первая версия модуля
  заявляла «ядро не останавливается» без этой проверки — исправлено до
  публикации (`30-08`).
* Попутно найден и исправлен баг: `flash()` не приводила путь к образу к
  абсолютному, а `run()` запускает CLI с cwd рабочей папки задания —
  относительный путь не находился (`30-09`).
* **Шаблон `templates/counter_l0`** — минимальная прошивка для
  Cortex-M0+/STM32L0: только растущий в ОЗУ счётчик, ни одного обращения
  к GPIO или другой периферии — безопасна для первой проверки записи на
  неизвестной или чужой плате.
* **`env.stlink_cli()` / `env.stlink_version()`** — поиск
  `STM32_Programmer_CLI.exe`: `MCUKIT_STLINK` → отдельная установка
  STM32CubeProgrammer → плагин `cubeprogrammer` внутри STM32CubeIDE.
* **База знаний**: `10-04` (CLI пакетно, успех по `Device ID`), `20-04`
  (выбор зонда по вендору чипа), `30-07` (`sn=` не работает в
  STM32CubeProgrammer 2.4.0), `30-08` (`connect` без `HOTPLUG` сбрасывает
  ядро), `30-09` (относительный путь к образу), `40-03` (установка,
  найденные пути, время операций, что проверено записью). `00_ПРАВИЛА.md`
  раздел 11 дополнен версиями второй проверки.
* **`SKILL.md`/`README.md`**: описание, триггеры, рабочий цикл и таблица
  возможностей — на оба зонда; правило «зонд по вендору чипа»; правило
  `mode=HOTPLUG` для чтения.

## 2026-09-25 — сырые уровни входов

* **`mk.sample(addr, count, interval_ms)`** — много чтений за один сеанс
  J-Link, ядро не останавливается. Приём — `20-03`: вход «не реагирует» —
  сначала сырые уровни, потом прошивка.

## 2026-09-25 — MCU-Kit 0.1.0

Первая версия. Всё ниже проверено на J-Link Software V9.38a (зонд J-Link
V9), arm-none-eabi-gcc 10.3.1 из STM32CubeIDE 1.12.1 и GD32F470 по SWD.

* **Библиотека `mcukit`**: J-Link Commander пакетно (`run`, `Result` с
  разбором эха команд и ошибок), `probe`, `info`, `read32`, `watch`,
  `save`, `reset_run`, `flash` с проверкой VTref и ответа ядра перед
  записью; сборка `build` копией проекта в безопасной папке, `size`,
  `symbol`, `vectors`; поиск J-Link, GCC и make, в том числе внутри
  STM32CubeIDE.
* **Шаблон `templates/blink_f4`**: blink без библиотек производителя для
  GD32F4/STM32F4 со счётчиком в ОЗУ для проверки отладчиком; `run.py` —
  сборка, запись, сверка, проверка счётчика.
* **База знаний**: 10-01…10-03, 20-01, 20-02, 30-01…30-06, 40-01, 40-02.
  Главные находки: без общей земли VTref показывает правдоподобное
  напряжение, а ядро не отвечает; профиль чипа GD32F4 без связи
  объявляет снятие защиты (стирание) — связь проверять профилем
  Cortex-M4; J-Link, спросив с консоли, молча обрывает сценарий с кодом
  0; GCC с `-Os` вставляет memcpy/memset в startup; binutils не
  открывают файлы по пути с кириллицей.
* **Оснастка** — как в соседних наборах: `tools/setup.ps1` (UTF-8 с
  BOM), `check_docs.py`, хук `commit-msg`, локальный стоп-лист, проверка
  автора коммитов.
* **Проверки**: `tests/test_build.py` — без железа; `tests/test_hw.py` —
  с платой, пишет во флеш.
